# PostgreSQL connection string
DATABASE_URL=postgres://ask-howard:ask-howard@localhost:5432/ask-howard?sslmode=disable

# JWT signing secret — must be set to a strong random value in production
JWT_SECRET=
